# Dev server (`npm run dev`) — talk to production API for live testing.
# Override locally: create `.env.development.local` (gitignored) with another URL if needed.
VITE_API_URL=https://apivnfast.patliputragroup.com/api/v1
